# Rust implementation reference

Rust is an implementation detail rather than the installed integration surface.
Version-matched crate documentation is still useful to contributors and adapter
authors and is built with every release.

[Open the generated rustdoc](https://docs.postproject.org/rustdoc/postproject_core/index.html).
