//! Rust examples for the `PostProject` guides.
//!
//! The examples live in `tests/*.rs` so that `cargo test --workspace` runs
//! every listing the documentation includes. Each `// [name]` ...
//! `// [/name]` region is extracted verbatim by the documentation build.
