[package]
name = "postproject-storage-sqlite"
description = "SQLite persistence backend for libpostproject"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
publish = false

[dependencies]
postproject-core = { version = "0.1.0-alpha.1", path = "../postproject-core" }
rusqlite = { version = "0.40.2", default-features = false, features = ["bundled"] }

[dev-dependencies]
postproject-media = { version = "0.1.0-alpha.1", path = "../postproject-media" }
tempfile = "3"

[lints]
workspace = true
