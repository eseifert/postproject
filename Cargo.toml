[workspace]
members = [
  "crates/postproject-cli",
  "crates/postproject-core",
  "crates/postproject-ffi",
  "crates/postproject-media",
  "crates/postproject-storage-sqlite",
]
resolver = "3"

[workspace.package]
version = "0.1.0-alpha.1"
edition = "2024"
rust-version = "1.85"
authors = ["libpostproject contributors"]
repository = "https://github.com/libpostproject/libpostproject"
license = "MIT OR Apache-2.0"

[workspace.lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"

[workspace.lints.clippy]
all = { level = "deny", priority = -1 }
pedantic = { level = "deny", priority = -1 }

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
